const {Router} = require('express');
const controller = require("./controller");

const router = Router();

router.get('/{{getLink}}', controller.get{{name}});
router.post('/{{postLink}}', controller.post{{name}} );
router.get('/{{popLink}}', controller.pop{{name}})

module.exports = router